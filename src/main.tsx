import React from 'react'
import ReactDOM from 'react-dom/client'
import AppNew from './AppNew'
// import App from './App' // 旧バージョン

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppNew />
  </React.StrictMode>
)